# Keep newly added dependencies exact instead of writing ^ ranges.
save-exact=true

# Avoid installing freshly published package versions. 10080 minutes = 7 days.
# `nub update --latest --exact` respects this cooling window, so upgrades happen
# only after a version has survived at least one week on the registry.
minimum-release-age=10080
